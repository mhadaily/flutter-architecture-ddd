name: flutter_architecture_ddd
description: Flutter Architecture inspired by Domain Driven Design, Onion and Clean Architecture.

version: 1.0.0+1

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  get_it: ^3.0.3
  flutter_bloc: ^2.0.1
  equatable: ^1.0.0
  data_connection_checker: ^0.3.4
  http: ^0.12.0+2
  shared_preferences: ^0.5.4+5

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
